version: '3'
services:
  bruschetta:
    build: .
    ports:
      - "3000:3000"
    env_file:
      - ./credentials.env
    links:
      - database
    depends_on:
      - database
  database:
    restart: always
    image: postgres:alpine
    ports:
      - "5432:5432"
    env_file:
      - ./credentials.env
    volumes:
      - /srv/docker/postgresql:/var/lib/postgresql